// Layer: landing
// Landing page pública do Zima — pré-renderizada (SSG) para PageSpeed e SEO máximos.
// Rota raiz "/", layout limpo sem sidebar/topbar do SaaS.
export default defineNuxtConfig({
  // Componentes, pages e composables são auto-importados via convenção de diretórios.
})
