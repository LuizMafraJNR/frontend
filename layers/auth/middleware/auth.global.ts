// Middleware global de autenticação — desabilitado temporariamente
// O SaaS está em fase de desenvolvimento sem backend/login implementado.
// TODO: reativar quando a tela de login estiver pronta (layers/auth/pages/login.vue)
export default defineNuxtRouteMiddleware(() => {
  // noop
})
